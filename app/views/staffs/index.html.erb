%h1 Listing staffs

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @staffs.each do |staff|
      %tr
        %td= link_to 'Show', staff
        %td= link_to 'Edit', edit_staff_path(staff)
        %td= link_to 'Destroy', staff, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Staff', new_staff_path
