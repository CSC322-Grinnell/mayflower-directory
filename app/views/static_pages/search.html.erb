<!DOCTYPE html>
<html>
<body>
 <br/>
<font size="7">Search Page</font>
 <br/>
 <font size="4">You don't have to fill in all these information to search. Fill in as much as you wish.
 <br/> If you provide no information, all residents will be shown in the result section below.</font>
 <br/>
 <br/>
</body>
</html>

<% @search = Profile.search(params[:q]) %>

<%= search_form_for @search, url: '/static_pages/search' do |f| %>

  <%= f.label :first_name_cont %>
  <%= f.search_field :first_name_cont %>
  <br/>
  <br/>
  <%= f.label :last_name_cont %>
  <%= f.search_field :last_name_cont %>
  <br/>
  <br/>
  <%= f.label :first_name_start %>
  <%= f.search_field :first_name_start %>
  <br/>
  <br/>
  <%= f.label :last_name_start %>
  <%= f.search_field :last_name_start %>
  <br/>
  <br/>
  <%= f.submit %>
  <br/>
  <br/>
  <br/>
<% end %>

<% @results = Profile.ransack(params[:q]).result %>
<% if @results.length > 0 %>
 <div class="one">
  <p> <font size="6"> Results: </font></p>
   <% @results.each do |person| %>
<p> <font size="4"> <%= person.first_name %> <%= person.last_name %> </font> </p>
<% end %>
   </div>
<% else %>
 <div class="two">No results. Try searching for something else.</div>
<% end %>
